/*
 * Modals Messages
 *
 * This contains all the text for the Modals container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'ncp.containers.Modals';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Modals container!',
  },
});
