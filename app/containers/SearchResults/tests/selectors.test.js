// import { selectSearchResultsDomain } from '../selectors';

describe('selectSearchResultsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
